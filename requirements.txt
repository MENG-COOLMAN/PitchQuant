# Football Odds Analysis Model — Python 依赖
# 说明: 核心流水线脚本（scripts/tmp/）以标准库为主，无第三方依赖即可运行。
#       以下为可选依赖，按需安装。

# ---- 可选: 在线学习模块 (scripts/online_learning/) ----
river>=0.21

# ---- 可选: 部分网络抓取脚本 ----
requests>=2.31

# ---- 可选: 数据处理（回测脚本）----
# pandas>=2.0
